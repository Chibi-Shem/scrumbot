import { GroupTimesheetsPipe } from './group-timesheets.pipe';

describe('GroupTimesheetsPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupTimesheetsPipe();
    expect(pipe).toBeTruthy();
  });
});
